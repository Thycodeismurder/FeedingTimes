import { TransformEventDataPipe } from './transform-event-data.pipe';

describe('TransformEventDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformEventDataPipe();
    expect(pipe).toBeTruthy();
  });
});
