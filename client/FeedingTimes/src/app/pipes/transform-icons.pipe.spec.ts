import { TransformIconsPipe } from './transform-icons.pipe';

describe('TransformIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
